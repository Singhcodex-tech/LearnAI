/**
 * LAW ASSIGNMENT GENERATOR — Isolated Feature Module
 * Mount: app.use('/api/assignment', require('./features/assignment'))
 * Remove: comment that line. Zero side-effects on host platform.
 */
const router = require('./routes/assignmentRouter');
module.exports = router;
